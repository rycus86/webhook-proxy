server:
  host: '0.0.0.0'
  port: '9999'

endpoints:
  - /docker:
      method: 'POST'

      headers:
        X-Test: '123'

      body:
        callback_url: 'https://.+'
        repository:
          repo_name: '^[0-9a-z_\\-]+/[0-9a-z_\\-]+$'
